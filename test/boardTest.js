const assert = require("chai").assert;
const { Gameboard } = require("../board.js");

describe("createBoard", () => {
  it("creates a board with 10 arrays", () => {

  });

  it("creates a board where each array contains 10 elements", () => {

  });
});